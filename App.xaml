<Application x:Class="DoAnNote.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DoAnNote"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             StartupUri="View/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <CornerRadius x:Key="RadiusMd">10</CornerRadius>

            <Style x:Key="RoundedButton" TargetType="Button">
                <Setter Property="Background" Value="#4F6DF5"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#4F6DF5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusMd}">
                                <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#3B57D6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="RoundedTextBox" TargetType="TextBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#D0D5DD"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"/>

                                    <TextBlock x:Name="Watermark"
                                   Text="{TemplateBinding Tag}"
                                   Foreground="#9AA3AF"
                                   Margin="{TemplateBinding Padding}"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   TextTrimming="CharacterEllipsis"
                                   Visibility="Collapsed"/>
                                </Grid>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="bd" Property="BorderBrush" Value="#4F6DF5"/>
                                    <Setter TargetName="bd" Property="BorderThickness" Value="2"/>
                                </Trigger>

                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>



            <Style x:Key="BackIconButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.85"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Opacity" Value="0.65"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>



            <DataTemplate x:Key="NoteCardTemplate">
                <Border CornerRadius="12" Padding="18" Margin="0,0,24,24"
            Background="{Binding Background}" >
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontSize="16"
                       FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding DateString}" Margin="0,12,0,0"
                       Foreground="#666666" FontSize="12"/>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <RadialGradientBrush x:Key="DropHaloBrush" RadiusX="0.85" RadiusY="0.65"
                     GradientOrigin="0.5,0.35" Center="0.5,0.5">
                <GradientStop Color="#30EF4444" Offset="0.00"/>
                <GradientStop Color="#10EF4444" Offset="0.40"/>
                <GradientStop Color="#00EF4444" Offset="1.00"/>
            </RadialGradientBrush>

            <RadialGradientBrush x:Key="CornerDeleteHaloBrush" RadiusX="0.85" RadiusY="0.65"
                     GradientOrigin="0.5,0.35" Center="0.5,0.5">
                <GradientStop Color="#30EF4444" Offset="0.00"/>
                <GradientStop Color="#10EF4444" Offset="0.40"/>
                <GradientStop Color="#00EF4444" Offset="1.00"/>
            </RadialGradientBrush>

            <RadialGradientBrush x:Key="CornerRestoreHaloBrush" RadiusX="0.85" RadiusY="0.65"
                     GradientOrigin="0.5,0.35" Center="0.5,0.5">
                <GradientStop Color="#3010B981" Offset="0.00"/>
                <GradientStop Color="#1010B981" Offset="0.40"/>
                <GradientStop Color="#0010B981" Offset="1.00"/>
            </RadialGradientBrush>


        </ResourceDictionary>
    </Application.Resources>
</Application>
